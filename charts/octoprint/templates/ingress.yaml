{{- if and .Values.octoprint.enabled .Values.octoprint.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "octoprint.fullname" . }}
  labels:
    {{- include "octoprint.octoprint.labels" . | nindent 4 }}
  {{- with .Values.octoprint.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.octoprint.ingress.className }}
  ingressClassName: {{ .Values.octoprint.ingress.className }}
  {{- end }}
  {{- if .Values.octoprint.ingress.tls }}
  tls:
    {{- range .Values.octoprint.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.octoprint.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "octoprint.fullname" $ }}
                port:
                  number: {{ $.Values.octoprint.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
