apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hdhomerun-app-proxy.fullname" . }}
  labels:
    {{- include "hdhomerun-app-proxy.labels" . | nindent 4 }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "hdhomerun-app-proxy.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "hdhomerun-app-proxy.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.hostNetwork }}
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      {{- end }}
      containers:
        - name: app-proxy
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["python", "hdhomerun_app_proxy.py"]
          {{- if .Values.bindAddress }}
          args: ["{{ .Values.bindAddress }}"]
          {{- end }}
          env:
            {{- if .Values.debug }}
            - name: DEBUG
              value: "True"
            {{- end }}
          ports:
            - name: tcp
              containerPort: 65001
              protocol: TCP
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
