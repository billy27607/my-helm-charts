apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "brave-remote.fullname" . }}
  labels:
    {{- include "brave-remote.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "brave-remote.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "brave-remote.selectorLabels" . | nindent 8 }}
    spec:
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      {{- if .Values.hostname }}
      hostname: {{ .Values.hostname }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: brave
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default "latest" }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: TZ
              value: {{ .Values.env.timezone | quote }}
            - name: PUID
              value: {{ .Values.env.puid | quote }}
            - name: PGID
              value: {{ .Values.env.pgid | quote }}
            - name: DISPLAY_WIDTH
              value: {{ .Values.env.displayWidth | quote }}
            - name: DISPLAY_HEIGHT
              value: {{ .Values.env.displayHeight | quote }}
            {{- range .Values.env.extraEnv }}
            - name: {{ .name }}
              value: {{ .value | quote }}
            {{- end }}
            {{- if .Values.auth.enabled }}
            - name: CUSTOM_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "brave-remote.fullname" . }}-auth
                  key: username
            - name: PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "brave-remote.fullname" . }}-auth
                  key: password
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
            {{- if .Values.service.vnc.enabled }}
            - name: vnc
              containerPort: {{ .Values.service.vnc.port }}
            {{- end }}
          volumeMounts:
            - name: config
              mountPath: {{ .Values.persistence.mountPath }}
            - name: dshm
              mountPath: /dev/shm
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
        - name: nginx-tls
          image: nginx:alpine
          ports:
            - name: https
              containerPort: 443
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/conf.d
            - name: tls-cert
              mountPath: /etc/nginx/ssl
              readOnly: true
      volumes:
        - name: config
          {{- if and .Values.persistence.enabled .Values.persistence.existingClaim }}
          persistentVolumeClaim:
            claimName: {{ .Values.persistence.existingClaim }}
          {{- else if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "brave-remote.fullname" . }}-config
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: {{ .Values.shm.sizeLimit }}
        - name: nginx-config
          configMap:
            name: {{ include "brave-remote.fullname" . }}-nginx
        - name: tls-cert
          secret:
            secretName: {{ .Values.ingress.tls.secretName | default (printf "%s-tls" (include "brave-remote.fullname" .)) }}
